<Window x:Class="VB6VisualMockupDesigner.StartScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="VB6 Visual Mockup Designer" 
        Height="600" Width="950"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        FontFamily="Segoe UI"
        Icon="/Resource/icon.ico"
        >

    <Window.Resources>
        <SolidColorBrush x:Key="BrandBrush" Color="#005A9E"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#3E3E42"/>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                           FontFamily="Segoe MDL2 Assets" 
                                           FontSize="20" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource BrandBrush}"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" 
                                               FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding ToolTip, RelativeSource={RelativeSource TemplatedParent}}" 
                                               FontSize="12" Foreground="#999999" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RecentItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" Margin="0,0,0,5">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Medium"/>

                                <TextBlock Text="{Binding FullPath}" FontSize="11" Foreground="#888888" TextTrimming="CharacterEllipsis"/>

                                <TextBlock Text="{Binding LastAccess, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" FontSize="11" Foreground="#666666"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="VB6 Visual Mockup Designer" FontSize="28" FontWeight="Light" Foreground="White"/>
            <TextBlock Grid.Row="1" Text="Abrir recientes" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,30,0,15"/>

            <TextBox Grid.Row="1" HorizontalAlignment="Right" Width="200" Margin="0,30,0,10"
                     Background="#333333" Foreground="White" BorderThickness="0" 
                     Padding="5" Text="Buscar recientes..." Opacity="0.5"/>

            <ListBox Grid.Row="2" x:Name="RecentProjectsList" 
                 Background="Transparent" BorderThickness="0" 
                 ItemContainerStyle="{StaticResource RecentItemStyle}"
                 SelectionChanged="RecentProjectsList_SelectionChanged">
                
            </ListBox>
            <StackPanel Grid.Row="2" x:Name="EmptyRecentState" 
            VerticalAlignment="Center" 
            HorizontalAlignment="Center"
            Visibility="Collapsed">
                <TextBlock Text="&#xE81C;" 
               FontFamily="Segoe MDL2 Assets" 
               FontSize="48" 
               Foreground="#3E3E42"
               HorizontalAlignment="Center" 
               Margin="0,0,0,15"/>

                <TextBlock Text="No hay elementos recientes" 
               FontSize="16" 
               Foreground="White" 
               FontWeight="SemiBold"
               HorizontalAlignment="Center"/>

                <TextBlock Text="Tus proyectos aparecerán aquí una vez que los abras." 
               FontSize="12" 
               Foreground="#888888" 
               HorizontalAlignment="Center" 
               Margin="0,5,0,0"
               TextWrapping="Wrap" 
               MaxWidth="250" 
               TextAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Border Grid.Column="1" Background="#252526" Padding="40">
            <StackPanel>
                <TextBlock Text="Empezar" FontSize="24" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>

                <Button Style="{StaticResource ActionButtonStyle}" 
                        Content="Crear nuevo mockup" 
                        ToolTip="Inicia un lienzo en blanco con controles VB6"
                        Tag="&#xE710;" 
                        Click="BtnNew_Click"
                        Margin="0,0,0,10"/>

                <Button Style="{StaticResource ActionButtonStyle}" 
                        Content="Abrir archivo .frm" 
                        ToolTip="Importar ingeniería inversa desde VB6"
                        Tag="&#xE8E5;" 
                        Click="BtnOpen_Click"
                        Margin="0,0,0,10"/>

                <Button Style="{StaticResource ActionButtonStyle}" 
                        Content="Abrir carpeta de proyecto" 
                        ToolTip="Escanear múltiples formularios"
                        Tag="&#xE8B7;" 
                        Margin="0,0,0,10"/>

                <Separator Background="#3E3E42" Margin="0,20"/>

                <TextBlock Text="Novedades v0.8" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                <TextBlock Text="• Selección Múltiple (Ctrl+Click)" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                <TextBlock Text="• Historial Deshacer/Rehacer" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                <TextBlock Text="• Exportación a PNG" Foreground="#AAAAAA" Margin="0,0,0,5"/>

            </StackPanel>
        </Border>

        <Button Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" 
                Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets"
                Background="Transparent" Foreground="#AAAAAA" BorderThickness="0"
                Width="40" Height="40" Click="BtnClose_Click" Margin="0,5,5,0"/>
    </Grid>
</Window>