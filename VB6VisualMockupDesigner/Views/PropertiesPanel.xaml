<UserControl x:Class="VB6VisualMockupDesigner.Views.PropertiesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:VB6VisualMockupDesigner.Models"
             xmlns:views="clr-namespace:VB6VisualMockupDesigner.Views"
             xmlns:converters="clr-namespace:VB6VisualMockupDesigner.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="300"
             FontFamily="Segoe UI"
             Background="{DynamicResource PanelBackground}"
             Foreground="{DynamicResource PrimaryText}">

    <UserControl.Resources>

        <converters:FontStringConverter x:Key="FontStringCleanConverter"/>

        <Style x:Key="ToolHeaderButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                            <TextBlock Text="{TemplateBinding Content}" 
                                       FontFamily="Segoe MDL2 Assets" 
                                       FontSize="12" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" SnapsToDevicePixels="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BrandColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource HeaderBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Height" Value="25"/>
        </Style>

        <Style TargetType="TextBox" x:Key="PropEditStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridCell}, Path=Foreground}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <DataTemplate x:Key="ColorEditorTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ToggleButton x:Name="ColorBtn" Grid.Column="0" Margin="0,1,4,1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="0" Cursor="Hand">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Border Background="{Binding Value}"/>
                            </Border>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                <Popup IsOpen="{Binding IsChecked, ElementName=ColorBtn, Mode=TwoWay}" StaysOpen="False" PlacementTarget="{Binding ElementName=ColorBtn}" Placement="Bottom">
                    <views:ColorPicker ColorSelected="OnColorPickedFromPopup"/>
                </Popup>
                <TextBox Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource PropEditStyle}"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="DialogEditorTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="25"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding Value}" Style="{StaticResource PropEditStyle}" IsReadOnly="True" Foreground="{DynamicResource SecondaryText}"/>
                <Button Grid.Column="1" Content="..." FontWeight="Bold" Padding="0,-2,0,0" Margin="0,1,1,1" 
                        Background="{DynamicResource HeaderBackground}" Foreground="{DynamicResource PrimaryText}" 
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                        Click="OnDialogButtonClick"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="FontEditorTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="25"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" 
                         Text="{Binding Value, Converter={StaticResource FontStringCleanConverter}}" 
                         Style="{StaticResource PropEditStyle}" 
                         IsReadOnly="True" 
                         Foreground="{DynamicResource SecondaryText}"/>

                <Button Grid.Column="1" Content="..." FontWeight="Bold" Padding="0,-2,0,0" Margin="0,1,1,1" 
                        Background="{DynamicResource HeaderBackground}" Foreground="{DynamicResource PrimaryText}" 
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                        Click="OnDialogButtonClick"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ValueCellTemplate" DataType="{x:Type models:PropertyItem}">
            <ContentControl Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Setter Property="ContentTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource PropEditStyle}"/>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="Boolean">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding Options}" SelectedItem="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource PropComboStyle}"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Enum">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding Options}" SelectedItem="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource PropComboStyle}"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Color">
                                <Setter Property="ContentTemplate" Value="{StaticResource ColorEditorTemplate}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="File">
                                <Setter Property="ContentTemplate" Value="{StaticResource DialogEditorTemplate}"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding Type}" Value="Font">
                                <Setter Property="ContentTemplate" Value="{StaticResource FontEditorTemplate}"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding Type}" Value="ReadOnly">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Value}" Opacity="0.7" Padding="0" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </DataTemplate>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" MinHeight="60" MaxHeight="150"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}" Padding="5" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Propiedades" FontWeight="Bold" FontSize="12" Margin="2,0,5,0" VerticalAlignment="Center"/>
                    <ComboBox x:Name="ObjectSelector" Width="140" Height="22" IsEnabled="True" FontWeight="Bold" Style="{DynamicResource PropComboStyle}" SelectionChanged="ObjectSelector_SelectionChanged"/>
                </StackPanel>

                <Button Grid.Column="1" Content="&#xE711;" Click="CloseBtn_Click" Style="{StaticResource ToolHeaderButtonStyle}"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource PanelBackground}" Padding="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,5,0">
                    <ToggleButton x:Name="BtnCategorized" IsChecked="True" Width="26" Height="24" ToolTip="Categorizado" Style="{StaticResource ToolbarToggleBtnStyle}" Click="SortButton_Click">
                        <TextBlock Text="&#xE8FD;" FontFamily="Segoe MDL2 Assets"/>
                    </ToggleButton>
                    <ToggleButton x:Name="BtnAlphabetical" Width="26" Height="24" ToolTip="Alfabético" Style="{StaticResource ToolbarToggleBtnStyle}" Click="SortButton_Click">
                        <TextBlock Text="&#xE8CB;" FontFamily="Segoe MDL2 Assets"/>
                    </ToggleButton>
                </StackPanel>

                <Grid Grid.Column="1">
                    <TextBox x:Name="SearchBox" Style="{StaticResource PropEditStyle}" Background="{DynamicResource HeaderBackground}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="20,2,2,2" VerticalContentAlignment="Center" TextChanged="SearchBox_TextChanged"/>
                    <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource SecondaryText}" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0" IsHitTestVisible="False"/>
                </Grid>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="PropGrid" AutoGenerateColumns="False" HeadersVisibility="None" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource BorderBrush}" Background="Transparent" RowBackground="Transparent" BorderThickness="0" CellEditEnding="PropGrid_CellEditEnding" SelectionChanged="PropGrid_SelectionChanged">
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource HeaderBackground}" Padding="5,2">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{DynamicResource SecondaryText}"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <StackPanel>
                                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"/>
                                            <ItemsPresenter />
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Width="110" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Width="*" CellTemplate="{StaticResource ValueCellTemplate}"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="3" Height="3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="{DynamicResource BorderBrush}"/>

        <Border Grid.Row="4" Background="{DynamicResource PanelBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="5">
            <StackPanel>
                <TextBlock x:Name="DescTitle" Text="Propiedad" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,2"/>
                <TextBlock x:Name="DescText" Text="Seleccione una propiedad para ver su descripción." FontSize="11" Foreground="{DynamicResource SecondaryText}" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>