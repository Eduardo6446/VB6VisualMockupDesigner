<Window x:Class="VB6VisualMockupDesigner.Views.StringListEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editor de Lista de Cadenas" 
        Height="400" Width="480"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize" 
        WindowStyle="None" 
        Background="{DynamicResource PanelBackground}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1">

    <Window.Resources>
        <Style x:Key="WindowCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" 
                                       FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C42B1C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryText}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="0,2">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                BorderThickness="0" 
                                Padding="{TemplateBinding Padding}" 
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="15"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="0,0,10,0" 
                                                  VerticalAlignment="Center" Visibility="Collapsed"/>

                                <ContentPresenter Grid.Column="1" ContentSource="Header" 
                                                  VerticalAlignment="Center" RecognizesAccessKey="True"/>

                                <Path x:Name="Arrow" Grid.Column="3" Data="M 0 0 L 4 3.5 L 0 7 Z" 
                                      Fill="{TemplateBinding Foreground}" VerticalAlignment="Center" 
                                      HorizontalAlignment="Center" Visibility="Collapsed"/>

                                <Popup x:Name="Popup" Placement="Right" IsOpen="{TemplateBinding IsSubmenuOpen}" 
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                    <Border Background="{DynamicResource PanelBackground}" 
                                            BorderBrush="{DynamicResource BorderBrush}" 
                                            BorderThickness="1" Margin="2,0,0,0">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Icon" Value="{x:Null}">
                                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{DynamicResource HeaderBackground}" 
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <TextBlock Text="Editor de Lista de Cadenas" 
                           Foreground="{DynamicResource PrimaryText}" 
                           VerticalAlignment="Center" 
                           Margin="10,0,0,0" 
                           FontSize="12"/>

                <Button HorizontalAlignment="Right" 
                        Style="{StaticResource WindowCloseButtonStyle}"
                        Click="BtnCancel_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" 
                Background="{DynamicResource HeaderBackground}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,0,0,1"
                Padding="15,10">
            <StackPanel>
                <TextBlock Text="Escribe las cadenas de la colección (una por línea):" 
                           Foreground="{DynamicResource PrimaryText}" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>
                <TextBlock Text="Presiona Enter para agregar un nuevo elemento." 
                           Foreground="{DynamicResource SecondaryText}" 
                           FontSize="11"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" 
                Margin="15"
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="1" 
                Background="{DynamicResource HeaderBackground}">
            <TextBox x:Name="TxtContent"
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" 
                     FontSize="13" 
                     Padding="5"
                     Background="Transparent"
                     Foreground="{DynamicResource PrimaryText}"
                     CaretBrush="{DynamicResource PrimaryText}"
                     BorderThickness="0"/>
        </Border>

        <Grid Grid.Row="3" Margin="15,0,15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="LblLineCount" 
                           Text="0 líneas" 
                           Foreground="{DynamicResource SecondaryText}" 
                           FontSize="11" 
                           VerticalAlignment="Center"
                           Margin="0,0,15,0"/>

                <Button Content="Insertar..." Width="80" Height="24" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        FontSize="11"
                        Click="BtnInsertMenu_Click">
                    <Button.ContextMenu>
                        <ContextMenu x:Name="MnuInsert">
                            <MenuItem Header="Días de la Semana" Click="InsertDays_Click"/>
                            <MenuItem Header="Meses del Año" Click="InsertMonths_Click"/>
                            <MenuItem Header="Alfabeto (A-Z)" Click="InsertAlphabet_Click"/>
                            <Separator/>
                            <MenuItem Header="Números (1-10)" Click="InsertNumbers10_Click"/>
                            <MenuItem Header="Números (0-9)" Click="InsertNumbers09_Click"/>

                            <Separator/>
                            <MenuItem x:Name="MnuUserPresets" Header="Mis Presets">
                                <MenuItem Header="(Vacío)" IsEnabled="False"/>
                            </MenuItem>
                            <MenuItem Header="Guardar selección como Preset..." Click="BtnSavePreset_Click"/>

                            <Separator/>
                            <MenuItem Header="Cargar desde Archivo..." Click="LoadFromFile_Click"/>
                            <MenuItem Header="Limpiar Todo" Click="Clear_Click"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
            </StackPanel>

            <Button Content="Aceptar" Click="BtnOk_Click" 
                    Width="90" Height="28" Margin="0,0,10,0" 
                    Grid.Column="1" IsDefault="True"
                    Background="{DynamicResource BrandColor}" Foreground="White" BorderThickness="0"/>

            <Button Content="Cancelar" Click="BtnCancel_Click" 
                    Width="90" Height="28" 
                    Grid.Column="2" IsCancel="True"
                    Style="{StaticResource SecondaryButtonStyle}"/>
        </Grid>
    </Grid>
</Window>